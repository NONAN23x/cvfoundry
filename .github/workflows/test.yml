name: test

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - run: docker build -t cvfoundry:test .
      - run: docker run --rm --entrypoint python3 -e RUN_LIBREOFFICE_INTEGRATION=1 cvfoundry:test -m unittest discover -s tests -v
